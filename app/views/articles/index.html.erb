<h1>All articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %> -
    <%= link_to "delete", article_path(article), method: :delete %></li>
  <% end %>
</ul>

<p><%= link_to "new article", articles_new_path(@article) %></p>
